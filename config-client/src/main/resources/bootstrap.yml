spring:
  application:
    name: didispace
  cloud:
    config:
      uri: http://localhost:7001/        #Config server的uri
      profile: dev                       #指定的环境
      label: master
server:
  port: 7002
management:
  security:
    enabled: false