spring:
  application:
    name: config-server
  cloud:
    config:
      server:
        git:
          uri: https://github.com/owengoodluck/spring-cloud-demo.git
          searchPaths: config-repo
#          username:
#          password:
server:
  port: 7001


#eureka:
#  client:
#    register-with-eureka: true
#    service-url:
#      defaultZone: http://peer1:1111/eureka
#    instance:
#      lease-renewal-interval-in-seconds: 10
